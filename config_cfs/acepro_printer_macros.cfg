# ============================================================================
# Generic Pause/Resume Macros (ACE-agnostic)
# V3.0 CFS Version
# ============================================================================
# Organized and maintained by Ducati_1198
# Shoutout to the original project: https://github.com/Kobra-S1/ACE_Pro
# ============================================================================
# External/consumer macros expected elsewhere:
# - CUT_TIP (optional if using PAUSE with cut_filament)
# - TO_THROW_POSITION (used during pause/resume purge)
# - RESUME_NOZZLE_WIPE_SEQUENCE (used during resume purge)
# - _ACE_STATE (optional: used in RESUME to reload tool when ACE enabled)
######################################################################

# Pause/Resume state that is not ACE-specific (also used when ACE is off)
[gcode_macro _PAUSE_RESUME_STATE]
variable_pause_nozzle_timeout_triggered: 0   # Flag: nozzle timeout has already lowered temperature (0/1)
variable_pre_pause_temp: 0                    # Extruder target temperature saved before PAUSE (°C)
gcode:
  # no gcode — just holds variables

[gcode_macro INSERT_EXTRUDE_50]
description: Extrude additional 50mm after filament insert
gcode:
    M83
    G92 E0
    G1 E50 F300
    G92 E0

[gcode_macro INSERT_RESUME_PRINT]
description: Resume print after filament insert prompt
gcode:
    RESPOND TYPE=command MSG=action:prompt_end
    RESUME

[gcode_macro INSERT_CANCEL_PRINT]
description: Cancel print from filament insert prompt
gcode:
    RESPOND TYPE=command MSG=action:prompt_end
    CANCEL_PRINT

[gcode_macro INSERT_LOAD_FILAMENT]
description: Load filament from idle insert prompt
gcode:
  RESPOND TYPE=command MSG=action:prompt_end
  LOAD_FILAMENT

[gcode_macro INSERT_DISMISS]
description: Dismiss filament insert dialog
gcode:
  RESPOND TYPE=command MSG=action:prompt_end

[gcode_macro RUNOUT_DISMISS]
description: Close runout dialog
gcode:
    RESPOND TYPE=command MSG=action:prompt_end

[gcode_macro RUNOUT_CANCEL_PRINT]
description: Cancel print from runout dialog
gcode:
    RESPOND TYPE=command MSG=action:prompt_end
    CANCEL_PRINT

#####################################################################################################################################
# JUST FOR SIMPLE AF PRINTER #### ADD THIS MACRO _SAF_ACE_TOOL_LOAD TO CUSTOM HOOKS [gcode_macro _SAF_START_PRINT_BEFORE_LINE_PURGE]
# Orca Slicer Machine Start G-Code: SET_GCODE_VARIABLE MACRO=_SAF_ACE_TOOL_LOAD VARIABLE=requested_tool VALUE={initial_tool}
######## BUT BETTER TO USE THE MY_PRINT_START MACROS ############
#####################################################################################################################################

[gcode_macro _SAF_ACE_TOOL_LOAD]
description: Load the requested ACE tool and wipe nozzle (used before purge line)
variable_requested_tool: -1   # Will be set by slicer start G‑code
gcode:
    {% set tool = params.TOOL|default(printer["gcode_macro _SAF_ACE_TOOL_LOAD"].requested_tool)|int %}
    {% if tool < 0 %}
        { action_respond_info("SAF ACE: No tool specified, skipping load") }
    {% else %}
        # Smart logic: check if tool already loaded
        {% set ace_index = printer.save_variables.variables.ace_current_index|default(-1)|int %}
        {% set filament_pos = printer.save_variables.variables.ace_filament_pos|default('none') %}
        {% set is_physically_loaded = printer["filament_switch_sensor filament_runout_nozzle"].filament_detected %}

        {% if tool == ace_index and filament_pos == 'nozzle' and is_physically_loaded %}
            { action_respond_info("SAF ACE: Tool %d already loaded – skipping" % tool) }
        {% else %}
            { action_respond_info("SAF ACE: Loading tool %d..." % tool) }
            # Load the tool (this calls ACE_CHANGE_TOOL, which includes purge and wipe)
            T{tool}
            M400
        {% endif %}
    {% endif %}

[gcode_macro PARK_FOR_CANCEL]
description: Simple park move for cancel (uses _CLIENT_VARIABLE coordinates)
gcode:
    {% if "xyz" in printer.toolhead.homed_axes %}
        {% set client = printer["gcode_macro _CLIENT_VARIABLE"] %}
        {% set park_x = client.park_at_cancel_x|default(300) %}
        {% set park_y = client.park_at_cancel_y|default(280) %}
        {% set speed = printer.configfile.settings.pause_resume.recover_velocity|default(50) * 60 %}
        G90
        G1 X{park_x} Y{park_y} F{speed}
        { action_respond_info("PARK_FOR_CANCEL: Parked at X%.1f Y%.1f" % (park_x, park_y)) }
    {% else %}
        RESPOND TYPE=error MSG="PARK_FOR_CANCEL: Printer not homed, cannot park"
    {% endif %}

######################################################################
# Enhanced Pause/Resume
######################################################################

[gcode_macro PAUSE]
rename_existing: BASE_PAUSE
variable_nozzle_timeout_seconds: 600
variable_standby_nozzle_temp: 150
gcode:
  
  ZERO_OUT_SERVO

  {% set z = params.Z|default(10)|int %}
  {% set cut_filament = params.CUT_FILAMENT|default(0)|int %}
  {% set standby = params.STANDBY|default(0)|int %}

  {% if printer['pause_resume'].is_paused|int == 0 %}
    SET_GCODE_VARIABLE MACRO=RESUME VARIABLE=zhop VALUE={z}
    SET_GCODE_VARIABLE MACRO=RESUME VARIABLE=etemp VALUE={printer['extruder'].target}

    {% set current_target = printer.extruder.target|int %}
    {% if current_target > 0 %}
      SET_GCODE_VARIABLE MACRO=_PAUSE_RESUME_STATE VARIABLE=pre_pause_temp VALUE={current_target}
      {action_respond_info("ANY[m]: PAUSE: Saved pre-pause temp: " ~ current_target ~ "°C")}
    {% endif %}

    BASE_PAUSE

    SAVE_GCODE_STATE NAME=PAUSE_LOCAL
      G90
      M82
      VEL_PUSH
      SET_VELOCITY_LIMIT ACCEL=5000 ACCEL_TO_DECEL=2500 VELOCITY=200 SQUARE_CORNER_VELOCITY=5

      # --- Extruder retraction (before any movement) ---
      {% set vars = printer["gcode_macro _ACE_VARS"] %}
      {% set retract_len = vars.pause_extruder_retract|float %}
      {% set retract_speed = vars.pause_extruder_retract_speed|float %}
      {% if retract_len > 0 %}
          { action_respond_info("PAUSE: Retracting " ~ retract_len|round(1) ~ "mm filament") }
          M83
          G1 E-{retract_len} F{retract_speed * 60}
          M82
          G92 E0
      {% endif %}

      {% if cut_filament == 1 %}
        {action_respond_info("ANY[m]: PAUSE: Cutting filament")}
        CUT_TIP
      {% else %}
        {action_respond_info("ANY[m]: PAUSE: Moving to throw position")}
      {% endif %}
      
      # --- Initial small Z-hop ---
      G91
      G1 Z3 F1500
      G90

      # ===== Clearance Z lift (servo or independent) – MUST be before TO_THROW_POSITION =====
      {% set current_z = printer.toolhead.position.z %}
      {% if vars.servo_enable|lower == 'true' %}
          {% if current_z < vars.servo_min_z %}
              M118 ACE: Raising bed to minimum servo height ({vars.servo_min_z}mm) before throw
              G0 Z{vars.servo_min_z} F{vars.servo_z_speed * 60}
          {% endif %}
          # Do not deploy servo here – that happens after throw
      {% else %}
          {% if vars.clearance_enable|lower == 'true' %}
              {% if current_z < vars.clearance_z_height %}
                  M118 ACE: Raising bed to clearance height ({vars.clearance_z_height}mm) before throw
                  G0 Z{vars.clearance_z_height} F{vars.clearance_z_speed * 60}
              {% endif %}
          {% endif %}
      {% endif %}
      
      # --- Now move to throw position safely ---
      TO_THROW_POSITION

      # --- Bed drop for clearance (servo or independent) – after throw, lower bed if needed ---
      {% if vars.servo_enable|lower == 'true' %}
          {% set bed_drop = vars.bed_pause_z_drop | float %}
          {% if bed_drop > 0 %}
              G91
              G1 Z{bed_drop} F{vars.servo_z_speed * 60}
              G90
              M400
          {% endif %}
          SET_SERVO SERVO={vars.servo_name} ANGLE={vars.servo_deploy_angle}
          G4 P{vars.servo_deploy_delay * 1000}
      {% else %}
          {% if vars.clearance_enable|lower == 'true' %}
              {% set bed_drop = vars.clearance_pause_z_drop | float %}
              {% if bed_drop > 0 %}
                  G91
                  G1 Z{bed_drop} F{vars.clearance_z_speed * 60}
                  G90
                  M400
              {% endif %}
          {% endif %}
      {% endif %}

      VEL_POP
      RESTORE_GCODE_STATE NAME=PAUSE_LOCAL MOVE=0

    {% if standby == 1 and printer['extruder'].target|int > 0 %}
      {% set target = printer['extruder'].target|int %}
      {% set standby_drop = 20 %}
      {% set standby_temp = (target - standby_drop) if (target - standby_drop) > 0 else 0 %}
      M104 S{standby_temp}
      {action_respond_info("ANY[m]: PAUSE: Immediate standby mode - nozzle at " ~ standby_temp ~ "°C")}
    {% else %}
      {% set timeout = printer["gcode_macro PAUSE"].nozzle_timeout_seconds|int %}
      {action_respond_info("ANY[m]: PAUSE: Nozzle will auto-cool to standby after " ~ timeout ~ " seconds")}
      UPDATE_DELAYED_GCODE ID=PAUSE_NOZZLE_TIMEOUT DURATION={timeout}
    {% endif %}

    SET_IDLE_TIMEOUT TIMEOUT=43200
    SET_GCODE_VARIABLE MACRO=_PAUSE_RESUME_STATE VARIABLE=pause_nozzle_timeout_triggered VALUE=0

  {% endif %}


[delayed_gcode PAUSE_NOZZLE_TIMEOUT]
gcode:
    {% if printer['pause_resume'].is_paused|int == 1 %}
        {% set standby_temp = printer["gcode_macro PAUSE"].standby_nozzle_temp|int %}
        { action_respond_info("ANY[m]: Pause timeout reached, lowering to standby temp %d°C" % standby_temp) }
        M104 S{standby_temp}
        SET_GCODE_VARIABLE MACRO=_PAUSE_RESUME_STATE VARIABLE=pause_nozzle_timeout_triggered VALUE=1
    {% endif %}

[gcode_macro RESUME]
rename_existing: BASE_RESUME
variable_zhop: 0
variable_etemp: 0
gcode:
    RESPOND TYPE=echo MSG="ANY[m] [RESUME] initiated"
    RESPOND TYPE=command MSG=action:prompt_end
    {% set client_vel = printer.configfile.settings.pause_resume.recover_velocity|default(50.0) %}
    {% set vars = printer["gcode_macro _ACE_VARS"] %}

    {% set default_purge = vars.resume_purge_amount|default(40)|float %}
    {% set purge_amount = params.PURGE|default(default_purge)|float %}
    {% set extra_prime = vars.resume_extra_prime|default(1.0)|float %}
    {% set pre_wipe_retract = vars.resume_post_wipe_retract|default(1.0)|float %}

    {% if printer['pause_resume'].is_paused|int == 1 %}
        UPDATE_DELAYED_GCODE ID=PAUSE_NOZZLE_TIMEOUT DURATION=0
        SET_IDLE_TIMEOUT TIMEOUT={printer.configfile.settings.idle_timeout.timeout}
        SAVE_GCODE_STATE NAME=RESUME_WRAP

        # --- Reheat nozzle if needed ---
        {% if etemp > 0 %}
            {% set timeout_triggered = printer["gcode_macro _PAUSE_RESUME_STATE"].pause_nozzle_timeout_triggered|int %}
            {% if timeout_triggered == 1 %}
                {% set msg = "ANY[m]: RESUME: Reheating nozzle from standby to " ~ etemp ~ "°C" %}
                RESPOND TYPE=echo MSG="{msg}"
            {% endif %}
            MOVE_HEAT_POS
            M109 S{etemp|int}
        {% endif %}

        # --- ACE tool reactivation ---
        {% set call_tool_change = false %}
        {% set ace_enabled = printer.configfile.settings.get('output_pin ACE_Pro', None) is not none %}
        {% if ace_enabled %}
            {% set active_tool = printer["gcode_macro _ACE_STATE"].active %}
            {% set filament_detected = false %}
            {% set sensor_exists = printer.configfile.settings.get('filament_switch_sensor filament_runout_nozzle', None) is not none %}
            {% if sensor_exists %}
                {% set filament_detected = printer["filament_switch_sensor filament_runout_nozzle"].filament_detected %}
            {% endif %}
            {% if active_tool >= 0 and not filament_detected %}
                {% set call_tool_change = true %}
                {% set msg = "ANY[m]: Will reload ACE tool T" ~ active_tool ~ " (no filament detected)" %}
                RESPOND TYPE=echo MSG="{msg}"
            {% elif active_tool >= 0 and filament_detected %}
                {% set msg = "ANY[m]: SKIPPING ACE reload – filament already present" %}
                RESPOND TYPE=echo MSG="{msg}"
                UPDATE_DELAYED_GCODE ID=SYNC_ACE_AFTER_MANUAL_LOAD DURATION=1
            {% endif %}
        {% endif %}

                # --- If we are calling T{active_tool}, let ACE handle purge/wipe (skip macro's own purge) ---
        {% set skip_purge = call_tool_change %}
            {% set msg = "ANY[m]: Skipping macro purge/wipe – ACE tool change will handle it" %}
            RESPOND TYPE=echo MSG="{msg}"
            {% if purge_amount > 0 and not skip_purge %}
            T{active_tool}
            M400
        {% endif %}

        # {% if call_tool_change %}
        #     {% set msg = "ANY[m]: Skipping macro purge/wipe – ACE tool change will handle it" %}
        #     RESPOND TYPE=echo MSG="{msg}"
        #     {% set purge_amount = 0 %}
        #     T{active_tool}
        #     M400
        # {% endif %}

        G92 E0
        RESPOND TYPE=echo MSG="ANY[m]: Extruder position reset to E=0"

        # --- Macro's own purge/wipe ---
        {% if purge_amount > 0 %}
            # ===== Clearance Z lift (servo or independent) – MUST be before TO_THROW_POSITION =====
            {% set current_z = printer.toolhead.position.z %}
            {% if vars.servo_enable|lower == 'true' %}
                {% if current_z < vars.servo_min_z %}
                    M118 ACE: Raising bed to minimum servo height ({vars.servo_min_z}mm) before throw
                    G0 Z{vars.servo_min_z} F{vars.servo_z_speed * 60}
                {% endif %}
            {% else %}
                {% if vars.clearance_enable|lower == 'true' %}
                    {% if current_z < vars.clearance_z_height %}
                        M118 ACE: Raising bed to clearance height ({vars.clearance_z_height}mm) before throw
                        G0 Z{vars.clearance_z_height} F{vars.clearance_z_speed * 60}
                    {% endif %}
                {% endif %}
            {% endif %}

            # --- Now move to throw position safely ---
            TO_THROW_POSITION

            {% if pre_wipe_retract > 0 %}
                {% set msg = "ANY[m]: Retracting " ~ pre_wipe_retract ~ "mm before wipe" %}
                RESPOND TYPE=echo MSG="{msg}"
                G91
                G1 E-{pre_wipe_retract} F600
                G90
            {% endif %}

            {% set was_abs_e = printer.gcode_move.absolute_extrude %}
            M83
            G1 E{purge_amount} F300
            {% if was_abs_e %} M82 {% endif %}

            # --- Hit purge chute ---
            HIT_PURGE_CHUTE

            SAVE_GCODE_STATE NAME=RESUME_LOCAL
            VEL_PUSH
            SET_VELOCITY_LIMIT ACCEL=5000 ACCEL_TO_DECEL=2500 VELOCITY=200 SQUARE_CORNER_VELOCITY=5
            RESUME_NOZZLE_WIPE_SEQUENCE
            FLUSH_POOP

            {% if vars.servo_enable|lower == 'true' %}
                SET_SERVO SERVO={vars.servo_name} ANGLE={vars.servo_retract_angle}
                G4 P{vars.servo_retract_delay * 1000}
                {% if vars.servo_poweroff_delay > 0 %}
                    UPDATE_DELAYED_GCODE ID=disable_servo_delayed DURATION={vars.servo_poweroff_delay / 1000}
                {% else %}
                    SET_SERVO SERVO={vars.servo_name} WIDTH=0
                {% endif %}
            {% endif %}

            SERVO_SHAKE
            VEL_POP
            RESTORE_GCODE_STATE NAME=RESUME_LOCAL MOVE=0
        {% else %}
            {% set msg = "ANY[m]: No purge/wipe needed (handled by ACE or manual load)" %}
            RESPOND TYPE=echo MSG="{msg}"
            {% if vars.servo_enable|lower == 'true' %}
                SET_SERVO SERVO={vars.servo_name} ANGLE={vars.servo_retract_angle}
                G4 P{vars.servo_retract_delay * 1000}
                {% if vars.servo_poweroff_delay > 0 %}
                    UPDATE_DELAYED_GCODE ID=disable_servo_delayed DURATION={vars.servo_poweroff_delay / 1000}
                {% else %}
                    SET_SERVO SERVO={vars.servo_name} WIDTH=0
                {% endif %}
            {% endif %}
        {% endif %}

        {% if extra_prime > 0 %}
            {% set msg = "ANY[m]: Extra prime " ~ extra_prime ~ "mm before resume" %}
            RESPOND TYPE=echo MSG="{msg}"
            G91
            G1 E{extra_prime} F300
            G90
            G92 E0
        {% endif %}

        G92 E0
        RESTORE_GCODE_STATE NAME=RESUME_WRAP MOVE=0
        BASE_RESUME VELOCITY={params.VELOCITY|default(client_vel)}
    {% else %}
        RESPOND TYPE=echo MSG="ANY[m] [RESUME] called, but not paused"
    {% endif %}

[delayed_gcode SYNC_ACE_AFTER_MANUAL_LOAD]
initial_duration: 0
gcode:
    {% set ace_enabled = printer.configfile.settings.get('output_pin ACE_Pro', None) is not none %}
    {% if ace_enabled %}
        {% set active_tool = printer["gcode_macro _ACE_STATE"].active %}
        {% if active_tool >= 0 %}
            RESPOND TYPE=echo MSG="ACE: Syncing state after manual load"
            ACE_DEBUG_SET_FILAMENT_STATE STATE=nozzle
            ACE_DEBUG_SET_CURRENT_INDEX TOOL={active_tool}
        {% endif %}
    {% endif %}

# [gcode_macro RESUME]
# rename_existing: BASE_RESUME
# variable_zhop: 0                          # Z-hop amount saved from PAUSE (mm)
# variable_etemp: 0                          # Extruder target temperature saved from PAUSE (°C)
# gcode:
#     { action_respond_info("ANY[m] [RESUME] initiated") }
#     #In case resume was called from RESUME PROMPT, close the prompt
#     RESPOND TYPE=command MSG=action:prompt_end
#     {% set client_vel = printer.configfile.settings.pause_resume.recover_velocity|default(50.0) %}
    
#     # Optional purge override parameter
#     {% set purge_amount = params.PURGE|default(40)|float %}

#     # Get servo variables early for later use
#     {% set vars = printer["gcode_macro _ACE_VARS"] %}

#     {% if printer['pause_resume'].is_paused|int == 1 %}
#         ; Cancel the nozzle timeout timer
#         UPDATE_DELAYED_GCODE ID=PAUSE_NOZZLE_TIMEOUT DURATION=0
        
#         ; Restore standard idle timeout now that we're resuming
#         SET_IDLE_TIMEOUT TIMEOUT={printer.configfile.settings.idle_timeout.timeout}

#         ; Wrap EVERYTHING we touch so modal F/coords/E-mode can't leak
#         SAVE_GCODE_STATE NAME=RESUME_WRAP

#         ; Reheat if needed (uses stored target from PAUSE)
#         {% if etemp > 0 %}
#           {% set timeout_triggered = printer["gcode_macro _PAUSE_RESUME_STATE"].pause_nozzle_timeout_triggered|int %}
#             {% if timeout_triggered == 1 %}
#                 { action_respond_info("ANY[m]: RESUME: Reheating nozzle from standby to=%f°C" % etemp) }
#             {% endif %}
#             MOVE_HEAT_POS
#             M109 S{etemp|int}
#         {% endif %}

#         ; Flag to track if resume should proceed
#         {% set resume_ok = true %}

#         ; Re-activate ACE tool if enabled
#         {% if printer["output_pin ACE_Pro"] is defined and printer["output_pin ACE_Pro"].value %}
#           { action_respond_info("ANY[m]: (Re)activate Tool") }
#           {% set active_tool = printer["gcode_macro _ACE_STATE"].active %}
#           {% if active_tool >= 0 %}
#               ; Attempt to load the tool
#               T{active_tool}
#               M400  ; Wait for all moves to complete
#           {% else %}
#             { action_respond_info("ANY[m]: active_tool=%s" % active_tool) }
#           {% endif %}
#         {% endif %}

#         G92 E0
#         { action_respond_info("ANY[m]: Extruder position reset to E=0") }

#         ; --- Purge and wipe (skip if PURGE=0) ---
#         {% if purge_amount > 0 %}
#             TO_THROW_POSITION
#             ; --- Small, safe prime with optional override ---
#             {% set was_abs_e = printer.gcode_move.absolute_extrude %}
#             M83
#             { action_respond_info("ANY[m]: Purging %fmm filament" % purge_amount) }
#             G1 E{purge_amount} F300
#             {% if was_abs_e %} M82 {% endif %}

#             # --- Perform full flush (moves + shake) ---
#             #FLUSH_POOP

#             # Enable this is wiping the nozzle at resume is prefered:
#             { action_respond_info("ANY[m]: [RESUME]Wipe nozzle") }
#             ; --- keep wipe/throw side-effects contained ---
#             SAVE_GCODE_STATE NAME=RESUME_LOCAL
#             VEL_PUSH
#             SET_VELOCITY_LIMIT ACCEL=5000 ACCEL_TO_DECEL=2500 VELOCITY=200 SQUARE_CORNER_VELOCITY=5
#             RESUME_NOZZLE_WIPE_SEQUENCE

#             # --- SERVO RETRACT ON RESUME ---
#             {% if vars.servo_enable | lower == 'true' %}
#                 SET_SERVO SERVO={vars.servo_name} ANGLE={vars.servo_retract_angle}
#                 G4 P{vars.servo_retract_delay * 1000}
#                 {% if vars.servo_poweroff_delay > 0 %}
#                     UPDATE_DELAYED_GCODE ID=disable_servo_delayed DURATION={vars.servo_poweroff_delay / 1000}
#                 {% else %}
#                     SET_SERVO SERVO={vars.servo_name} WIDTH=0
#                 {% endif %}
#             {% endif %}

#             G1 E1 F300
#             VEL_POP
#             RESTORE_GCODE_STATE NAME=RESUME_LOCAL MOVE=0
#             ; ---------------------------------------------
#         {% else %}
#             { action_respond_info("ANY[m]: Skipping purge and wipe (PURGE=0)") }
#             # Even if no purge, we should still retract the servo if it was deployed
#             {% if vars.servo_enable | lower == 'true' %}
#                 SET_SERVO SERVO={vars.servo_name} ANGLE={vars.servo_retract_angle}
#                 G4 P{vars.servo_retract_delay * 1000}
#                 {% if vars.servo_poweroff_delay > 0 %}
#                     UPDATE_DELAYED_GCODE ID=disable_servo_delayed DURATION={vars.servo_poweroff_delay / 1000}
#                 {% else %}
#                     SET_SERVO SERVO={vars.servo_name} WIDTH=0
#                 {% endif %}
#             {% endif %}
#         {% endif %}

#         G92 E0
#         { action_respond_info("ANY[m]: Extruder position reset to E=0 (final)") }
#         RESTORE_GCODE_STATE NAME=RESUME_WRAP MOVE=0

#         {% if printer["output_pin ACE_Pro"] is defined and printer["output_pin ACE_Pro"].value %}
#           { action_respond_info("ANY[m]: ACE resume finished") }
#         {% endif %}
#         BASE_RESUME VELOCITY={params.VELOCITY|default(client_vel)}
#     {% else %}
#       { action_respond_info("ANY[m] [RESUME] called, but currently not in PAUSED state") }
#     {% endif %}
    
######################################################################
# Velocity stack (shared)
######################################################################

[gcode_macro VEL_PUSH]
variable_stack: []                        # Stack of (accel, accel_to_decel, velocity, scv) tuples
gcode:
  {% set cur = printer["gcode_macro VEL_PUSH"].stack %}
  {% set s = cur + [(printer.toolhead.max_accel,
                     (printer.toolhead.max_accel_to_decel|default(printer.toolhead.max_accel, true)),
                     printer.toolhead.max_velocity,
                     printer.toolhead.square_corner_velocity)] %}
  SET_GCODE_VARIABLE MACRO=VEL_PUSH VARIABLE=stack VALUE="{s|tojson}"

[gcode_macro VEL_POP]
gcode:
  {% set cur = printer["gcode_macro VEL_PUSH"].stack %}
  {% if cur|length == 0 %}
    {action_respond_info("VEL_POP: stack empty, nothing to restore")}
  {% else %}
    {% set a, atd, v, scv = cur[-1] %}
    SET_VELOCITY_LIMIT ACCEL={a} ACCEL_TO_DECEL={atd} VELOCITY={v} SQUARE_CORNER_VELOCITY={scv}
    {% set s = cur[:-1] %}
    SET_GCODE_VARIABLE MACRO=VEL_PUSH VARIABLE=stack VALUE="{s|tojson}"
  {% endif %}


######################################################################
# Purge Logic (shared)
######################################################################

[gcode_macro PURGE_IN_CHUNKS]
description: Purges, splits long extrudes into smaller ones and poops. Accounts for pre-purged amount.
gcode:
  # --- Configurable purge ---
  {% set purge = params.PURGELENGTH|default(100)|float %}
  {% set speed = params.PURGESPEED|default(300)|float %}
  {% set already_purged = params.ALREADY_PURGED|default(0)|float %}
  {% set max_chunk = params.PURGE_MAX_CHUNK_LENGTH|default(300)|float %}
  
  # Calculate total amount that will be purged (for chunk boundary calculations)
  {% set total_purge = purge + already_purged %}
  
  # Calculate how many chunks we need for the TOTAL amount
  {% set full_chunks = (total_purge / max_chunk)|int %}
  
  # Calculate how much of the first chunk was already done
  {% set first_chunk_done = already_purged % max_chunk %}
  {% set first_chunk_remaining = max_chunk - first_chunk_done %}
  
  # If no purge needed
  {% if purge <= 0 %}
    {action_respond_info("ANY[m]: No purge needed")}
  
  # If total is less than one chunk
  {% elif full_chunks == 0 %}
    {action_respond_info("ANY[m]: Purging single chunk: %.1fmm (%.1fmm already done)" % (purge, already_purged))}
    PURGE_AND_POOP PURGELENGTH={purge} PURGESPEED={speed}
  
  # Multiple chunks needed - account for chunk boundaries
  {% else %}
    {% set remaining_to_purge = purge %}
    
    # First partial chunk (if we're in the middle of one)
    {% if first_chunk_done > 0 and first_chunk_remaining <= remaining_to_purge %}
      {action_respond_info("ANY[m]: Completing chunk %d: %.1fmm (%.1fmm already done)" % (1, first_chunk_remaining, first_chunk_done))}
      PURGE_AND_POOP PURGELENGTH={first_chunk_remaining} PURGESPEED={speed}
      {% set remaining_to_purge = remaining_to_purge - first_chunk_remaining %}
      {% set chunk_offset = 1 %}
    {% else %}
      {% set chunk_offset = 0 %}
    {% endif %}
    
    # Remaining full chunks
    {% set remaining_full_chunks = (remaining_to_purge / max_chunk)|int %}
    {% for i in range(remaining_full_chunks) %}
      {action_respond_info("ANY[m]: Purging chunk %d/%d: %.1fmm" % (
        i + 1 + chunk_offset, 
        remaining_full_chunks + chunk_offset + (1 if (remaining_to_purge - (remaining_full_chunks * max_chunk)) > 0 else 0),
        max_chunk))}
      PURGE_AND_POOP PURGELENGTH={max_chunk} PURGESPEED={speed}
    {% endfor %}
    
    # Final remainder chunk
    {% set final_remainder = remaining_to_purge - (remaining_full_chunks * max_chunk) %}
    {% if final_remainder > 0 %}
      {action_respond_info("ACE[M]: Purging final chunk %d: %.1fmm" % (
        remaining_full_chunks + chunk_offset + 1,
        final_remainder))}
      PURGE_AND_POOP PURGELENGTH={final_remainder} PURGESPEED={speed}
    {% endif %}
  {% endif %}


[gcode_macro PURGE_AND_POOP]
description: Purge
gcode:
  # --- Configurable purge ---
  {% set purge = params.PURGELENGTH|float %}
  {% set speed = params.PURGESPEED|float %}
  M83
  G92 E0  
  G1 E{purge*0.90} F{speed}
  G1 E{purge*0.10} F{speed*1.25}
  G1 E-1 F{speed*1.25}
  G92 E0
  G1 E1 F{speed*1.25}


######################################################################
# Filament load / unload
######################################################################


[gcode_macro LOAD_FILAMENT]
description: Load filament
gcode:
  SAVE_GCODE_STATE NAME=LOAD_FILAMENT_STATE
  {% if "xyz" not in printer.toolhead.homed_axes %}
      G28
  {% endif %}
  TO_THROW_POSITION

  {% set min_ext = printer.configfile.settings.extruder.min_extrude_temp|default(210)|int %}
  {% set cur_temp = printer.extruder.temperature|float %}
  {% if cur_temp < min_ext %}
    {action_respond_info("ANY[m]: Heating nozzle to 245°C for load (current=%.1f°C, min=%d°C)" % (cur_temp, min_ext))}
    M109 S245
  {% else %}
    {action_respond_info("ANY[m]: Nozzle already above min_extrude_temp (%.1f°C >= %d°C)" % (cur_temp, min_ext))}
  {% endif %}

  M83
  G92 E0
  M400
  {action_respond_info("ANY[m]: Extruding 150mm...")}
  G1 E150 F300
  G92 E0

  RESTORE_GCODE_STATE NAME=LOAD_FILAMENT_STATE MOVE=0

[gcode_macro UNLOAD_FILAMENT]
description: Unload filament with cutter and retract
gcode:
  SAVE_GCODE_STATE NAME=UNLOAD_FILAMENT_STATE
  {% if "xyz" not in printer.toolhead.homed_axes %}
      G28
  {% endif %}

  CUT_TIP
  M400
  {action_respond_info("ANY[m]: Retracting 100mm of filament...")}  
    M302 P1
    G91
    G1 E-100 F600
    G90
    M302 P0
  #FORCE_MOVE STEPPER="extruder" DISTANCE=-100 VELOCITY=10
  RESTORE_GCODE_STATE NAME=UNLOAD_FILAMENT_STATE MOVE=0

# Anycubic Slicer Next emits M900 gcode instead using the Klipper standard SET_PRESSURE_ADVANCE
# Lets map M900 to SET_PRESSURE_ADVANCE here and hope the given K value is compatible...
[gcode_macro M900]
description: "Marlin M900 compatibility: map Linear Advance K to Klipper SET_PRESSURE_ADVANCE"
gcode:
  {% set k = params.K|default(none) %}
  {% set l = params.L|default(none) %}
  {% set s = params.S|default(none) %}
  {% set t = params.T|default(none) %}

  # If no K/L provided, behave like "report"
  {% if k is none and l is none %}
    {% set e = printer.toolhead.extruder %}
    {% set pa = printer[e].pressure_advance|default(none) %}
    {% set st = printer[e].pressure_advance_smooth_time|default(none) %}
    {action_respond_info("M900 report -> EXTRUDER=%s pressure_advance=%s smooth_time=%s" % (e, pa, st))}
  {% else %}
    # Choose value: prefer K; fall back to L if K not provided
    {% set val = (k if k is not none else l) | float %}

    # Warn about Marlin-only concepts
    {% if s is not none %}
      {action_respond_info("M900 note: Klipper has no K-slot concept; ignoring S=%s" % s)}
    {% endif %}
    {% if t is not none %}
      {action_respond_info("M900 note: Klipper targets extruders by name, not T index; ignoring T=%s (using active extruder)" % t)}
    {% endif %}

    # Apply to currently active extruder unless you want to hardcode EXTRUDER=...
    SET_PRESSURE_ADVANCE ADVANCE={val}
    {action_respond_info("M900 -> SET_PRESSURE_ADVANCE ADVANCE=%.6f (active extruder)" % val)}
  {% endif %}


# ============================================================
# In Orca Slicer Machine start G-Code

# MY_START_PRINT TOOL={initial_tool} BED_TEMP=[bed_temperature_initial_layer_single] EXTRUDER_TEMP=[nozzle_temperature_initial_layer]
# SET_PRINT_STATS_INFO TOTAL_LAYER=[total_layer_count]
# ============================================================

[gcode_macro MY_START_PRINT]
description: Load tool via ACE (smart or manual), cool down, then START_PRINT
gcode:
    # Read adaptive bed mesh setting from virtual pin
    {% set ADAPTIVE = printer["output_pin bed_mesh_adaptive"].value | int %}
    
    {% set TOOL = params.TOOL|default(-1)|int %}
    {% set MATERIAL = params.MATERIAL|default("") %}
    {% set COLOR = params.COLOR|default("") %}
    {% set BED_TEMP = params.BED_TEMP|default(60)|int %}
    {% set EXTRUDER_TEMP = params.EXTRUDER_TEMP|default(220)|int %}
    {% set ace_enabled = printer["output_pin ACE_Pro"] is defined and printer["output_pin ACE_Pro"].value %}

    {% if not ace_enabled %}
        RESPOND MSG="ACE Pro disabled – passing through to START_PRINT"
        START_PRINT {rawparams}
    {% else %}
        # Capture original velocity limits
        {% set orig_max_velocity = printer.toolhead.max_velocity %}
        {% set orig_max_accel = printer.toolhead.max_accel %}
        {% set orig_scv = printer.toolhead.square_corner_velocity %}

        # Home first if not already
        {% if "xyz" not in printer.toolhead.homed_axes %}
            G28
        {% endif %}

        # --- Smart tool loading ---
        {% if MATERIAL != "" and COLOR != "" %}
            RESPOND MSG="MY_START_PRINT: Using SMART_TOOL with {MATERIAL} #{COLOR}"
            SMART_TOOL MATERIAL={MATERIAL} COLOR={COLOR}
        {% elif TOOL >= 0 %}
            RESPOND MSG="MY_START_PRINT: Loading tool T{TOOL}"
            DEPLOY_SERVO_AT_THROW
            M140 S{BED_TEMP}
            {% set min_extrude = printer.configfile.settings.extruder.min_extrude_temp|default(170)|int %}
            {% set load_temp = [EXTRUDER_TEMP, min_extrude]|max %}
            M109 S{load_temp}
            {% if printer["gcode_macro _ACE_STATE"] is defined %}
                SET_GCODE_VARIABLE MACRO=_ACE_STATE VARIABLE=startup_toolchange VALUE=1
            {% endif %}
            T{TOOL}
            M400
            {% if printer["gcode_macro _ACE_STATE"] is defined %}
                SET_GCODE_VARIABLE MACRO=_ACE_STATE VARIABLE=startup_toolchange VALUE=0
            {% endif %}
            ZERO_OUT_SERVO
        {% else %}
            RESPOND MSG="MY_START_PRINT: No tool specified – skipping load"
        {% endif %}

        # --- Post‑load cooldown (only if we actually loaded a tool) ---
        {% if TOOL >= 0 or (MATERIAL != "" and COLOR != "") %}
            RESPOND MSG="Filament ready – retracting servo and cooling down"

            # Safety Z before moving
            {% set vars = printer["gcode_macro _ACE_VARS"] %}
            {% set current_z = printer.toolhead.position.z %}
            {% if current_z < vars.servo_min_z %}
                RESPOND MSG="Raising bed to {vars.servo_min_z} mm for safety"
                G90
                G0 Z{vars.servo_min_z} F{vars.servo_z_speed * 60}
                M400
            {% endif %}

            MOVE_HEAT_POS

            RESPOND MSG="Cooling nozzle to 148°C"
            M106 S255
            M109 S148
            M106 S0

            ZERO_OUT_SERVO
            SERVO_SHAKE
        {% else %}
            RESPOND MSG="No filament loaded – skipping cooldown"
        {% endif %}

        # Restore original velocity limits
        SET_VELOCITY_LIMIT VELOCITY={orig_max_velocity} ACCEL={orig_max_accel} SQUARE_CORNER_VELOCITY={orig_scv}

        START_PRINT {rawparams}
    {% endif %}

# [gcode_macro MY_START_PRINT]
# description: Load tool via ACE, cool down, then START_PRINT
# gcode:
#     # Read adaptive bed mesh setting from virtual pin
#     {% set ADAPTIVE = printer["output_pin bed_mesh_adaptive"].value | int %}

#     {% set TOOL = params.TOOL|default(0)|int %}
#     {% set BED_TEMP = params.BED_TEMP|default(60)|int %}
#     {% set EXTRUDER_TEMP = params.EXTRUDER_TEMP|default(200)|int %}
#     {% set ace_enabled = printer["output_pin ACE_Pro"] is defined and printer["output_pin ACE_Pro"].value %}

#     {% if not ace_enabled %}
#         RESPOND MSG="ACE Pro disabled – passing through to START_PRINT"
#         START_PRINT {rawparams}
#     {% else %}
#         # Capture original velocity limits
#         {% set orig_max_velocity = printer.toolhead.max_velocity %}
#         {% set orig_max_accel = printer.toolhead.max_accel %}
#         {% set orig_scv = printer.toolhead.square_corner_velocity %}
#         {% set orig_max_accel_to_decel = printer.toolhead.max_accel_to_decel|default(none) %}

#         # Home first if not already
#         {% if "xyz" not in printer.toolhead.homed_axes %}
#             G28
#         {% endif %}

#         # --- Smart skip: check if tool already loaded ---
#         {% set ace_index = printer.save_variables.variables.ace_current_index|default(-1)|int %}
#         {% set filament_pos = printer.save_variables.variables.ace_filament_pos|default('none') %}
#         {% set is_physically_loaded = False %}
#         {% if "filament_switch_sensor filament_runout_nozzle" in printer.configfile.settings %}
#             {% set is_physically_loaded = printer["filament_switch_sensor filament_runout_nozzle"].filament_detected %}
#         {% endif %}

#         {% set full_load_performed = False %}

#         {% if TOOL == ace_index and filament_pos == 'nozzle' and is_physically_loaded %}
#             RESPOND MSG="MY_START_PRINT: Tool T{TOOL} already loaded – skipping tool change"
#         {% else %}
#             RESPOND MSG="MY_START_PRINT: Loading tool T{TOOL} (Target: {EXTRUDER_TEMP}°C, Bed: {BED_TEMP}°C)"

#             DEPLOY_SERVO_AT_THROW

#             # --- Start bed heating (no wait) and heat nozzle for loading (wait) ---
#             M140 S{BED_TEMP}                     # Start bed heating in background
#             {% set min_extrude = printer.configfile.settings.extruder.min_extrude_temp|default(170)|int %}
#             {% set load_temp = [EXTRUDER_TEMP, min_extrude]|max %}
#             RESPOND MSG="Heating nozzle to {load_temp}°C for loading (bed heating to {BED_TEMP}°C in background)"
#             M109 S{load_temp}                    # Wait for nozzle to reach loading temp

#             # --- Set startup_toolchange flag so ACE aborts on failure ---
#             {% if printer["gcode_macro _ACE_STATE"] is defined %}
#                 SET_GCODE_VARIABLE MACRO=_ACE_STATE VARIABLE=startup_toolchange VALUE=1
#             {% endif %}
            
#             G4 P1000

#             T{TOOL}   # If this succeeds, filament is loaded. If it fails, macro stops here.
#             M400

#             # --- Clear the flag ---
#             {% if printer["gcode_macro _ACE_STATE"] is defined %}
#                 SET_GCODE_VARIABLE MACRO=_ACE_STATE VARIABLE=startup_toolchange VALUE=0
#             {% endif %}

#             {% set full_load_performed = True %}
#         {% endif %}

#         # --- Post‑load actions: only if we performed a full load ---
#         {% if full_load_performed %}
#             RESPOND MSG="Filament ready – retracting servo and cooling down"

#             # Safety Z before moving
#             {% set vars = printer["gcode_macro _ACE_VARS"] %}
#             {% set current_z = printer.toolhead.position.z %}
#             {% if current_z < vars.servo_min_z %}
#                 RESPOND MSG="Raising bed to {vars.servo_min_z} mm for safety"
#                 G90
#                 G0 Z{vars.servo_min_z} F{vars.servo_z_speed * 60}
#                 M400
#             {% endif %}

#             MOVE_HEAT_POS

#             RESPOND MSG="Cooling nozzle to 148°C"
#             M106 S255
#             M109 S148
#             M106 S0

#             ZERO_OUT_SERVO
#             SERVO_SHAKE
#         {% else %}
#             RESPOND MSG="Tool already loaded – skipping cooldown and cleanup"
#             # Minimal safety: ensure Z is not dangerously low
#             {% set vars = printer["gcode_macro _ACE_VARS"] %}
#             {% set current_z = printer.toolhead.position.z %}
#             {% if current_z < 10 %}
#                 G90
#                 G0 Z10 F600
#             {% endif %}
#         {% endif %}

#         # Restore original velocity limits (with conditional ACCEL_TO_DECEL)
#         {% if orig_max_accel_to_decel is not none %}
#             SET_VELOCITY_LIMIT VELOCITY={orig_max_velocity} ACCEL={orig_max_accel} ACCEL_TO_DECEL={orig_max_accel_to_decel} SQUARE_CORNER_VELOCITY={orig_scv}
#         {% else %}
#             SET_VELOCITY_LIMIT VELOCITY={orig_max_velocity} ACCEL={orig_max_accel} SQUARE_CORNER_VELOCITY={orig_scv}
#         {% endif %}
       
#         START_PRINT {rawparams}
#     {% endif %}
